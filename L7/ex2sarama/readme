Handlers и Processes - приложения, обмениваются данными через Kafka. Оба приложения запускаются в кубере. Код лежит в handlers и processes соответственно. 
На Handlers крутится http сервер: 
    "/rate", - принимает post запрос для отправки числа в kafka. Далее process принимает данные обрабытывает и отправляет в kafka 
    "/total", - get запрос; принимает данные через kafka от process, считает их скользящее среднее и отвечает этим значением на get запрос.
    "/hc" - healthcheck для работы с кубером